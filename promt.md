Read-only. One file only: backend/src/Casrr.Infrastructure/SqlServer/SqlReviewRepository.cs
Find the method SaveCustomerInfoAsync. Paste its complete SQL UPDATE statement and the parameter mappings. Nothing else. Do not summarise.

Read-only. Paste one complete existing lookup end-to-end as a template:
1) backend/src/Casrr.Api/Controllers/LookupsController.cs — the method serving GET /api/v1/lookups/relationship/segments
2) The corresponding service method in ReportingService.cs
3) The corresponding repository method + SQL in the SqlReportingRepository implementation
Paste raw code for all three. Nothing else.
