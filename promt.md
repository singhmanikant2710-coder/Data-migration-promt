Checklist Questionnaire report — the header shows date AND time ("9/17/2026, 7:43:51 PM"). Client wants date only, matching every other CRM report's header pattern (formatDate strips time, as used in CrmSummaryPDF/ReviewSummaryForManagementPDF).

Find the header component in ChecklistQuestionnairePDF.tsx — is it using formatDate(generatedOn) (which should already strip time, per the pattern used elsewhere) or is it using a raw toLocaleString() / similar that includes time? Show the exact line. Fix to match the date-only pattern used in the other reports (e.g. formatDate from pageSetup.ts). Show diff, do NOT commit.
