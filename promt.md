"Resolved. The Filter rows search now updates the Progress Status grid counts and Totals live. Counts reflect the current My View scope and search text; they reset when the search is cleared. Note: counts intentionally do not collapse when you click a status in the grid, so the grid remains usable for navigation.
Re: the Clear button question — there is no Clear button on the Review Queue screen; that note appears to have carried over from item #31 (Customer Background Comments). Please confirm."
