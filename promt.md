Backend build succeeded and the endpoint GET /api/v1/customer-info/lookups/naics-industry is now live and returning distinct values.

Proceed with the frontend service helper:
- Create frontend/src/services/customerInfoLookupsService.ts with getNaicsIndustryOptions() as planned.
- Single-file edit only. Show me the file content, then wait for my confirmation before moving to CustomerInfoSection.tsx.
