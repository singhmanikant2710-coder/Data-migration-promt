Phase 2 — Bug 191. Apply the glyph fallback. At the 17 base Page style sites (the verified list from Phase 1), change fontFamily: "Helvetica" → fontFamily: ["Helvetica", "DejaVuSans"]. ONLY the base Page/Document style fontFamily. Do NOT touch any other style, spacing, HtmlRichText, or the 3 WinAnsi-safe extra occurrences in CroProductionSummaryPDF that aren't base Page styles. Use the verified 17-file list. Show all diffs. Commit: "Fix Bug 191: add DejaVuSans glyph fallback so ≥/≤ render as symbols (metric-neutral, Helvetica primary)".
