READ-ONLY. Diagnostics only. Do NOT change anything.

File: frontend/src/app/load-samples/page.tsx

Show me ONLY (no edits):
1. The FULL "Select Sample" card structure — from its outermost wrapping <div> (the one with the dark blue "Select Sample" header bar) all the way through the filters row, the grid/table, and pagination — down to its closing tag. I need to see every nested div and its className.
2. The FULL "Load Samples" card structure similarly — from its outermost wrapper through its header bar, grid, and pagination.
3. Where exactly did the min-w-[900px] wrapper get added in the last edit — show that specific spot in context, so I can see if it's wrapping only the inner DataTable or a larger section.

Read once. Findings only. No edits.
