File: frontend/src/app/review-history/page.tsx + the DataTable component

READ-ONLY. Do NOT edit. Answer in plain text only:

1. In the columns array, the borrower name and the pdf/document icon — are they 
   in the SAME column object's render (one <td>), OR are they TWO SEPARATE 
   column objects (two separate <td> cells)?

2. If they are in the SAME single column render: paste that column's full 
   definition exactly (key, className, cellClassName, and the full render JSX).

3. Confirm the exact column "key" or header label for the borrower-name column, 
   and tell me its current cellClassName value verbatim.

Report only. No edits.
