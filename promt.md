Apply the diff exactly as shown. Only edit computeConsumerFinancePercentOverride in frontend/src/app/blackbook/edit/page.tsx. Replace the label-branching if/else so that ONLY Net C/O % (matching /^net\s*c\/o/i or /charge[-\s]?off/i) sets numeratorAliases; all other labels (including Cash Collections % and 60+ DPD %) hit the else branch and return null. Keep the rest of the function (num/denom/return) unchanged. Apply now, then confirm Cash Collections % and 60+ DPD % return null and only Net C/O % is overridden.
