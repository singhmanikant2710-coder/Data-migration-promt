Show me the complete content of both files you just created:
1. backend/src/Casrr.Application/IReviewHistoryRepository.cs 
   (full content, including the ReviewHistoryRow DTO)
2. backend/src/Casrr.Infrastructure/SqlServer/SqlReviewHistoryRepository.cs 
   (full content, including the complete SQL query with the JOIN)

Then run a build check using the terminal:
dotnet build backend/src/Casrr.Api/Casrr.Api.csproj

Show me the build output — confirm there are no errors.

Do not modify any other files.
