READ-ONLY. Diagnostics only. Do NOT change anything.

File: frontend/src/components/pdf/CrmSummaryTablePDF.tsx

Show me ONLY (no edits):
1. The component's props/type definition — does it receive `generatedOn` (or meta.generatedOn / data.meta.generatedOn) anywhere? Show the exact prop/field.
2. Is there a formatRunDate or formatDate helper already imported/available in THIS file? Show the import line.

Read once. Findings only.
