SELECT COLUMN_NAME, DATA_TYPE FROM INFORMATION_SCHEMA.COLUMNS
WHERE TABLE_NAME = '02_CORE_02_Reviews'
  AND (COLUMN_NAME LIKE '%rationale%' OR COLUMN_NAME LIKE '%general%' 
       OR COLUMN_NAME LIKE '%transactional%' OR COLUMN_NAME LIKE '%unsat%comment%')
ORDER BY COLUMN_NAME;
