Approved — this is the correct fix. The cascade effects should depend on the in-edit value from FormChangesContext (currentSegment/currentUnit), not the saved r object. Apply the minimal change to CustomerInfoSection.tsx as described. Keep the useRef initial-load guards and the [], [currentSegment], [currentSegment, currentUnit] dependencies.
