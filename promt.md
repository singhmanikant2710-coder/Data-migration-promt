READ-ONLY. Diagnostics only. Do NOT change anything.

File: frontend/src/components/pdf/CrmSummaryPDF.tsx

Show me ONLY the full set of column width classes for the Scorecard Assessment table — sc1 through sc8 (or however many exist) — with their exact flexBasis/width percentage values. I need all of them so the total adds to 100%.

Also show the exact line where scorecardId is rendered (the .replace(...) call).

Read once. Findings only. No edits.
