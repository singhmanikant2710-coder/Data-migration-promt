READ-ONLY. Diagnostics only. Do NOT change anything.

File: frontend/src/components/pdf/CrmPdGradeMigrationPDF.tsx

Show me ONLY (no edits):

1. The complete DetailTablePages function as it is NOW. I need to confirm groups.map appears ONCE (not duplicated). Show the full return.

2. The complete DetailTablePage function as it is NOW — confirm it accepts filters + isLast and renders the "Applied Report Filters" block only when isLast.

3. The has-items branch Document return as it is NOW — confirm the separate filter Page is gone and DetailTablePages is called with filters. Show it.

4. Confirm there is only ONE has-items Document return (the diff showed two different-looking returns — clarify which is real).

Read once. Findings only. No edits.
