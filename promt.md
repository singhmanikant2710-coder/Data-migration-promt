Single-file edit: frontend/src/components/pdf/CrmPdGradeMigrationPDF.tsx

The commitment matrix section title currently reads "PD Grade Migration by 
Scorecard Commitment". Geoff wants "Scorecard" removed.

Find the MatrixCommitment section heading (the <Text style={styles.sectionTitle}> 
that says "PD Grade Migration by Scorecard Commitment") and change it to:
  "PD Grade Migration by Commitment"

CONSTRAINTS:
- Change ONLY this one heading text. Remove the word "Scorecard" only.
- Do NOT touch the count matrix heading, data, colors, or any other section.
- Do NOT loop or change anything else. One text change, then stop.
- Only edit this one file. Show the changed line.
