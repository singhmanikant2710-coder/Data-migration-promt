STOP reading files. Just run ONE command and show output only:
grep -rl "Review_finalized_date" backend/src --include=*.cs
grep -rl "Unlock" frontend/src --include=*.tsx

Show only the file paths that match. Do NOT open or read the files. Just the 
list of file names.
