
Two things before final approval:
1. Your re-verification table labels Imperial Trading 202510 and 202603
   as "Confirmed legacy" — but the only legacy Access data we've seen for
   Imperial Trading ends at 202104 (35 total records, starting 201806).
   Where did the "confirmed legacy" value for these two rows actually
   come from? If it was an assumption/extrapolation rather than an actual
   observed row, relabel it as such — don't mark unverified values as
   confirmed.
2. Row 1 of the table shows "start=1" for Athens Paper monthKey 202510 —
   Athens's actual start month is 10 (correctly shown in row 2). The
   computed fy=2026 confirms start=10 was actually used in the
   calculation, so this looks like a table transcription error, not a
   computation error. Please confirm and correct the table.
