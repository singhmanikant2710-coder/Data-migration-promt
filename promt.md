Apply the diff exactly as shown. Only change the onSaveCovenantOrder POST call in frontend/src/app/customer/edit/page.tsx to append ?customer=${encodeURIComponent(qpName)} to the URL. Keep the payload body unchanged. Apply now and confirm.
