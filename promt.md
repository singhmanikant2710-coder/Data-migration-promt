Read-only. No edits. No plan. No summary.

Paste, verbatim, ONLY these two things:

A) frontend/src/app/review-status/page.tsx — copy the code from the line `return (` in the main page component down to and including the NEXT 3 opening JSX tags after it, with their full className attributes.

B) frontend/src/app/review-queue/page.tsx — copy the code from the line `return (` in the main page component down to and including the NEXT 3 opening JSX tags after it, with their full className attributes.

Do not summarise. Do not describe. Paste the raw code only.
